//%attributes = {}
//var $searches : cs.SearchSelection
//$searches:=ds.Search.all()
//var $search : cs.SearchEntity
//For each ($search; $searches)
//$search.similarity:=$search.embeddings.cosineSimilarity($search.passage.embeddings)
//$search.save()
//End for each 